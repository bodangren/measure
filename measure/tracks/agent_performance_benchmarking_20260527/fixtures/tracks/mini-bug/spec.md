# Spec: Mini Bug (fixture)

Deterministic 2-task bug track used as a fixture for the
`agent_performance_benchmarking_20260527` Phase 1 harness tests.

## Acceptance Criteria
- [ ] Exposes exactly two tasks in `plan.md`
- [ ] Tasks use the standard `[ ]` markers (no `[x]`, no `[~]`)
- [ ] Title contains the word "Bug" so the runner can group by type
